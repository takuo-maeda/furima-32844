    <% if Card.pluck(:user_id).include?(current_user.id) %>

本当にを削除しますか？<br>

    【カード番号】
    <%= "**** **** **** " + @card[:last4] %>
    <%# カード番号の下4桁を取得 %>
    <br>
    【有効期限】
    <%= @card[:exp_month] %>
    <%# 有効期限の「月」を取得 %>
    /
    <%= @card[:exp_year] %>

    <%# <%= @card %>
    <%= link_to '削除する', card_path(@card.id), method: :delete %>
    <%= link_to '戻る', root_path %>
    <% else %>
    カード登録がまだのようです。
    カード登録をお願いします。
    <%= link_to '登録画面へ', new_card_path %>
    <%= link_to '戻る', root_path %>
    <% end %>